億家 App v0.10.10.5 Point Discount Quick Select

新增：
- 點數折抵頁新增快速選擇。
- 依 TM / SC 現行折抵單位自動產生 1倍、2倍、3倍。
- 例如目前 300 點折 $1，就顯示 300 / 600 / 900 點。
- 點數不足的按鈕自動停用。
- 「最多」會自動選擇目前餘額可使用的最大完整折抵單位。
- 顯示目前最多可使用點數與預估折抵金額。
- 規則仍直接同步 TM / SC，不把 300 點寫死。

此版純前端，不需 SQL。
只需上傳 index.html。
