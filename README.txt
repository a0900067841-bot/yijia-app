億家 App v0.10.10.7 Pay + Points Combined UI

依參考畫面調整：
- 點數折抵頁改成「億家錢包 / 點數折抵」整合式介面。
- 上方頁籤可切回億家錢包。
- 顯示目前可用點數。
- 顯示會員條碼。
- 顯示目前點數。
- 新增「折抵」勾選。
- 折抵金額依選擇點數即時計算。
- 顯示即將到期點數。
- 保留 1倍 / 2倍 / 3倍 / 最多 快速選擇。
- 未勾選折抵時不可產生 YPD QR。
- TM / SC 點數同步與規則不變。

此版純前端，不需 SQL。
直接上傳 index.html。
